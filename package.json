{
  "name": "claim-processor-cdk",
  "version": "0.1.0",
  "description": "Claim Processor infrastructure — AWS CDK (TypeScript). S3 bucket + presigned-URL upload API.",
  "bin": {
    "claim-processor-cdk": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.7.0",
    "aws-cdk": "^2.160.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "aws-cdk-lib": "^2.160.0",
    "constructs": "^10.3.0"
  }
}
